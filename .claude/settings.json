{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"'); cd /Users/sleepycoffee/Projects/Code/teta && git add -A && git diff --cached --quiet || { git commit -m \"Update ${f##*/}\" && git push origin HEAD; } 2>/dev/null || true",
            "statusMessage": "Pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
